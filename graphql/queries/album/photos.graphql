query Photos($where: WherePhotoInput) {
    photos(where: $where) {
        id
        media {
            id
            url
        }
        metadata
        tags {
            name
        }
    }
}