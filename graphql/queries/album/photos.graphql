query Photos($where: WherePhotoInput, $orderBy: PhotoOrderBy) {
    photos(where: $where, orderBy: $orderBy) {
        id
        media {
            id
            url
        }
        metadata
        tags {
            name
        }
    }
}